<div class="row">
  <div class="col-md-8">
   <% @items.each do |item| %>
     <h3><%= item.name %> | <%= item.user.email%></h3>
     <%= link_to "", item, method: :delete, remote: true, class: 'glyphicon glyphicon-ok' %>
   <% end %>
  </div>
</div>
